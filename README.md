🩺 Doctor-Visit 🏥

یک سیستم ساده برای مدیریت ملاقات‌ها و پرداخت‌های ویزیت‌های پزشکان. این پروژه با استفاده از زبان برنامه‌نویسی پایتون و یک معماری لایه‌بندی شده (MVC/Layered Architecture) توسعه یافته است.
🌟 ویژگی‌های کلیدی
* مدیریت ویزیت‌ها: ثبت، مشاهده، و به‌روزرسانی اطلاعات مربوط به ملاقات‌های بیماران با پزشک.
* مدیریت پرداخت‌ها: ثبت و پیگیری پرداخت‌های انجام شده برای ویزیت‌ها.
* معماری ماژولار: استفاده از ساختار controller, model, و view برای جداسازی مسئولیت‌ها و نگهداری آسان‌تر کد.
🛠️ پیش‌نیازها
قبل از شروع، مطمئن شوید که موارد زیر را نصب کرده‌اید:
* پایتون (Python): نسخه‌ی 3.x
* PIP (Package Installer for Python)
🚀 نصب و راه‌اندازی
برای راه‌اندازی این پروژه در محیط محلی خود، مراحل زیر را دنبال کنید:
1. شبیه‌سازی مخزن (Clone the Repository)
git clone https://github.com/fatemehahang/Doctor-visit.git
cd Doctor-visit

2. ایجاد محیط مجازی (Virtual Environment) (اختیاری اما توصیه شده)
# در ویندوز
python -m venv venv
.\venv\Scripts\activate

# در لینوکس/مک
python3 -m venv venv
source venv/bin/activate

3. نصب وابستگی‌ها (Dependencies)
پروژه به کتابخانه‌های خاصی نیاز دارد. آن‌ها را با استفاده از فایل requirements.txt نصب کنید:
pip install -r requirements.txt

> توجه: اگر فایل requirements.txt ندارید، باید کتابخانه‌های مورد استفاده (مانند tkinter، ماژول دیتابیس، یا ابزارهای دیگر) را دستی نصب کنید.
>
4. راه‌اندازی دیتابیس (Database Setup)
این پروژه از یک دیتابیس برای ذخیره داده‌ها استفاده می‌کند (احتمالاً SQLite که در پوشه db پیکربندی شده است).
* اگر از SQLite استفاده می‌کنید: فایل دیتابیس به‌طور خودکار هنگام اجرای برنامه ایجاد می‌شود.
* اگر از دیتابیس دیگری استفاده می‌کنید: باید تنظیمات اتصال دیتابیس را در پوشه db یا فایل‌های تنظیمات محیطی به‌روز کنید.
⚙️ ساختار پروژه

پروژه بر اساس یک الگوی لایه‌ای سازماندهی شده است:

| controller | مدیریت جریان برنامه و منطق کسب و کار (Business Logic). |

| model | حاوی entity (تعاریف شیء داده)، repository (عملیات دیتابیس) و service (منطق پیچیده). |

| view | رابط کاربری گرافیکی (GUI) که با استفاده از tkinter یا ابزار مشابه ساخته شده است. |

| db | تنظیمات و اسکریپت‌های مربوط به اتصال و ساختار دیتابیس. |

| test | فایل‌های آزمایشی برای اطمینان از صحت عملکرد ماژول‌ها. |

| tools | ابزارهای کمکی و کلاس‌های عمومی. |

▶️ نحوه اجرای برنامه
برای اجرای برنامه اصلی، دستور زیر را اجرا کنید:

python main.py

> توجه: بسته به نقطه ورود برنامه شما، ممکن است نام فایل اجرایی اصلی شما متفاوت باشد.
>
📝 تست‌ها

شما می‌توانید برای اطمینان از عملکرد صحیح توابع کنترلر، تست‌های موجود در پوشه test را اجرا کنید.
# دستور اجرای تست‌ها (ممکن است با توجه به کتابخانه تست شما تغییر کند)
python -m unittest discover test

🤝 مشارکت

اگر مایل به کمک به توسعه این پروژه هستید، لطفاً:
* مخزن را فورک (Fork) کنید.
* تغییرات خود را در یک شاخه جدید (Branch) ایجاد کنید (git checkout -b feature/AmazingFeature).
* تغییرات خود را کامیت (Commit) کنید (git commit -m 'Add some AmazingFeature').
* شاخه را پوش (Push) کنید (git push origin feature/AmazingFeature).
* یک درخواست کشش (Pull Request) باز کنید.
